Random Snippets, code, commands, configs, etc that I have used and reused.  Now all in one place for reference.  

Note: File extensions don't necessarily correspond to the the file type, mostly just prefixed for syntax highlighting in TextMate.

